<%= @flat.name %>
<img src="<%=@flat.picture_url%>" alt="">
<%= link_to "delete this flat", flat_path(@flat), method: :delete %>

<%= simple_form_for :search, method ="GET", do |f| %>
  <%= f.input :name %>
  <%= f.button :submit, "search", class: "btn btn-primary" %>
<% end %>



<%# 10 - Filtering flats (Optional)
This can get you started @flats = Flat.where("name LIKE '%garden%'"), make sure you understand this statement before going any further.
How can we make sure the page still works like a traditional index, even if the user isn’t searching anything?
How can we make sure the input is prefilled with the search query once the user searched? %>
